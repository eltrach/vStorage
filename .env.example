# Nginx Uploads Server Environment Configuration
# Copy this file to .env and modify values as needed

# Host port mapping (optional - defaults to 80)
# The port on your host machine that will be mapped to container port 80
HOST_PORT=80

# Host uploads path (optional - defaults to ./uploads)
# The path on your host machine where uploaded files will be stored
# This will be mounted as /srv/uploads inside the container
HOST_UPLOADS_PATH=./uploads

# Nginx config path (optional - defaults to ./nginx)
# The path on your host machine where nginx configuration files are located
# This will be mounted as /etc/nginx inside the container
NGINX_CONFIG_PATH=./nginx

# Coolify Deployment Notes:
# When deploying in Coolify, you'll typically use absolute paths like:
# HOST_UPLOADS_PATH=/data/uploads
# NGINX_CONFIG_PATH=/data/nginx-uploads
# HOST_PORT= (not needed - Traefik routes internally)
