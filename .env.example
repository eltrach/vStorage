# Nginx Uploads Server Environment Configuration
# Copy this file to .env and modify values as needed

# Host uploads path (required for Coolify, optional for local)
# The directory on your host machine that contains files to serve
# For Coolify: Use the absolute path to your uploads directory
# For local: Use ./public or any local directory
HOST_UPLOADS_PATH=./public

# Host port to publish the Nginx server on (host:container -> EXPORTED_PORT:80)
EXPORTED_PORT=4444

# Coolify Deployment Example:
# In Coolify, set this environment variable to:
# HOST_UPLOADS_PATH=/home/nirra.chat/
#
# Note: Nginx configuration is baked into the Docker image.
# Do not mount config files as volumes in Coolify!
